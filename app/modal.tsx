import { TodoFormScreen } from '@/lib/screens/TodoFormScreen';

/**
 * Add todo modal route
 * Displays form to create a new todo
 */
export default function Modal() {
  return <TodoFormScreen mode="add" />;
}
