import { TodoFormScreen } from '../../lib/screens/TodoFormScreen';

/**
 * Edit todo route
 * Displays form to edit an existing todo
 */
export default function EditTodo() {
  return <TodoFormScreen mode="edit" />;
}
